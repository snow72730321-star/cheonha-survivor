# v14.4.2 배속·히든 HUD·에셋 로더 핫픽스

- 전투 HUD에서 누락된 `speedBtn`을 복구했습니다. 1× → 1.5× → 2× → 3× 순환 로직은 기존 fixed-step 배속 시스템을 그대로 사용합니다.
- 히든 무공 진행 조건 HUD와 레벨업 단서 배너를 전투 시작/종료, 중도 포기, 결과 화면에서 메뉴 복귀 시 강제로 비우도록 `clearHiddenProgressUi()`를 추가했습니다.
- 이미지 프리로더는 `?assetBuild=`가 붙은 요청이 Safari/PWA 캐시 상태에서 실패할 경우 동일 파일의 순수 상대경로로 한 번 재시도한 뒤에만 실패 처리합니다.
- 시작 시 실패 경고는 더 이상 모든 이미지를 'PNG'라고 뭉뚱그리지 않고 실제 실패 파일명을 표시합니다.
- 서비스워커 캐시 키와 GameAssets 빌드 키를 v14.4.2로 갱신했습니다.
