# v14.3.4 모바일 오디오 성능·효과음 교체 패치

## 렉 원인

v14.3.3은 효과음별로 여러 `HTMLAudioElement`와 `MediaElementSourceNode`를 생성했고, 공격·타격·아이템 획득이 겹칠 때 재생 요청과 오디오 노드가 빠르게 증가했다. 또한 게임 프레임마다 Gain 값과 볼륨 UI DOM을 함께 갱신했다.

## 변경 사항

- SFX를 최초 1회 디코딩한 뒤 `AudioBuffer`로 재사용
- 전역 동시 재생 10개, 전투 6개, UI 3개, 컷신 4개 제한
- 효과음별 쿨다운·우선순위·최대 인스턴스 적용
- 낮은 우선순위 효과음이 포화 상태에서 자동 생략되도록 처리
- 프레임마다 실행되던 볼륨 UI 갱신 제거
- 오디오 상태 확인을 180ms 간격으로 제한
- 기본 공격과 일반 타격 효과음의 중복 빈도 감소
- 카타나 연속 베기 내부 중복 재생 제거
- 44.1kHz 스테레오 정적 WAV 39개로 전면 교체
- BGM 서비스 워커 전체 캐시 제거 및 Range 스트리밍 유지

## 배포

v14.3.3 저장소 루트에 패치 ZIP의 파일을 그대로 덮어쓴다. 기존 탭을 완전히 닫고 다시 열어 `cheonha-v14-3-4-audio-performance-hq` 서비스 워커가 활성화되도록 한다.
