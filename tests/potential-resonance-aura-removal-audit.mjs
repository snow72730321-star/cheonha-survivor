import fs from "node:fs";
import assert from "node:assert/strict";
const visuals=fs.readFileSync("js/render/weapon-visuals-v14-4.js","utf8");
const forge=fs.readFileSync("js/systems/forge-v13.js","utf8");
assert.doesNotMatch(visuals,/function\s+resonance\s*\(/);
assert.doesNotMatch(visuals,/resonance-/);
assert.doesNotMatch(visuals,/dataset\.resonance\s*=\s*resonance/);
assert.doesNotMatch(visuals,/setLineDash\(res/);
assert.match(visuals,/function tier\(item\).*lv>=15\?3:lv>=10\?2:lv>=5\?1:0/);
assert.match(forge,/function applyPotentialResonance\(item\)/);
assert.match(forge,/player\.damageMul\*=1\.06/);
console.log("potential-resonance-aura-removal-audit: OK");
