import fs from "node:fs";
import assert from "node:assert/strict";
const storage=fs.readFileSync("js/systems/storage-forge.js","utf8");
const events=fs.readFileSync("js/ui/meta-menus-events.js","utf8");
const html=fs.readFileSync("index.html","utf8");
assert.match(storage,/const ORE_SYNTH_COUNT=5/);
assert.match(storage,/common:20,rare:85,epic:360,unique:1500,legendary:6200,mythic:25000,eternal:90000/);
assert.match(storage,/function nextSynthGrade\(grade\).*gradeIndex\("mythic"\)/s);
assert.match(storage,/function synthesizeOre\(/);
assert.match(storage,/function sellOre\(/);
assert.doesNotMatch(storage,/quench/);
assert.doesNotMatch(events,/\$\("quench"\)/);
assert.doesNotMatch(html,/id="quench"/);
for(const [lo,hi] of [[20,85],[85,360],[360,1500],[1500,6200],[6200,25000]]) assert.ok(hi<lo*5,"합성 판매 차익 금지");
console.log("ore-management-quench-removal-audit: OK");
