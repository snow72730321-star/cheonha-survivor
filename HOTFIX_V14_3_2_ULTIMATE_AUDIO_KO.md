# 천하생존록 v14.3.2 절기 오디오 긴급 수정

## 증상

절기 버튼 사용 시 다음 예외와 함께 게임 루프가 중단되었습니다.

`TypeError: GameAudio.play is not a function`

## 원인

각성 컷신 모듈이 구형 `GameAudio.play()` API를 호출했지만, v14.3.1 오디오 매니저의 공개 API에는 `playSFX()`와 `playUI()`만 포함되어 있었습니다.

## 수정

- 각성 컷신은 `playSFX()`를 우선 사용하고, API 존재 여부를 검사합니다.
- 오디오 매니저에 구형 호출용 `play()` 호환 별칭을 공개합니다.
- 실제 SFX 파일이 없으면 기존 요구대로 오류 없이 무음 처리합니다.
- 버전화된 컷신 파일 경로를 사용해 기존 서비스 워커 캐시와 섞여도 즉시 새 코드를 불러옵니다.
- JS/CSS/매니페스트는 서비스 워커에서 네트워크 우선으로 갱신합니다.
