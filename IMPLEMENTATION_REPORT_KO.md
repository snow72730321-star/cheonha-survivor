# 천하생존록 v14 개선 적용 보고서

## 적용 범위

이 배포본은 기존 v13의 게임 규칙과 저장 데이터를 최대한 유지하면서 안정성, 성능, 확장성, 접근성, 배포 구조를 보강한 호환형 v14 빌드다.

### 긴급 오류와 데이터 보호

- 존재하지 않는 `openLevelUp` 참조를 제거하고 오디오를 이벤트 방식으로 연결했다.
- 한 번에 여러 경지가 상승해도 선택권이 사라지지 않도록 `pendingLevelUps` 대기열을 추가했다.
- 대장간 스크립트가 저장 데이터를 읽기 전에 빈 계정을 저장할 수 있던 초기화 순서 오류를 제거했다.
- 저장 버전 14, 필드 타입·범위 검증, 식별자·문자열 정규화, 체크섬을 추가했다.
- 주 저장 손상 시 백업을 자동 복구하며, 손상된 주 저장이 정상 백업을 덮어쓰지 않게 했다.
- 세이브 내보내기·가져오기·초기화와 저장 실패 알림을 추가했다.
- 중도 포기와 정상 종료의 원정 집계를 하나의 종료 경로로 통합했다.
- 사용자 가져오기 파일을 통한 마크업 삽입을 줄이기 위해 데이터 정규화와 CSP를 적용했다.

### 실행 구조와 성능

- 판정은 60Hz 고정 틱, 화면은 30/60 FPS 선택형으로 분리했다.
- 탭 복귀 시 과도한 누적 업데이트를 제한했다.
- 적 탐색에 128px 셀 기반 Spatial Hash Grid를 적용했다.
- 투사체와 파티클 객체 풀, 품질별 개수 제한을 추가했다.
- 무작위 전체 배열 정렬을 표본 선택 방식으로 교체했다.
- 능력치 상한과 난이도·적 수치를 중앙 데이터 파일로 이동했다.
- 런별 피해 출처, 최대 객체 수, 평균·최저 FPS, 최악 프레임 통계를 추가했다.

### 콘텐츠와 에셋 확장

- 8개 무기군과 혈마를 `SkillRegistry`, `BossRegistry`에 등록했다.
- 기존 32×40·방향별 4프레임 시트를 그대로 지원한다.
- 선택적으로 48×64 행동별 시트(`idle`, `walk`, `dodge`, `hit`, `skill`, `dead`)를 등록할 수 있는 애니메이션 컨트롤러를 추가했다.
- 스프라이트 크기, 화면 오프셋, 피격 반경, 충돌 반경을 분리했다.
- 필수 이미지 사전 로딩과 진행률을 추가했으며, 외부 PNG 누락 시 구형 코드 생성 캐릭터로 자동 대체하지 않고 오류를 표시한다.

### UI와 접근성

- 왼손잡이 배치, 진동, 조이스틱·버튼 크기, 키 설정을 추가했다.
- 모션·섬광 감소, 절기 컷신 단축·생략, 고대비, 피해 문자·보스 방향표 크기를 추가했다.
- 히든 무공 조건이 일정 수준 이상 진행되면 전투 HUD에 진행도를 표시한다.
- 개발 통계는 설정에서 켠 경우에만 결과 화면에 표시한다.

### PWA와 문서

- 모든 스크립트, CSS, 캐릭터·초상화·적 이미지를 앱 셸에 포함했다.
- 문서 요청에만 `index.html`을 대체 응답으로 사용하도록 서비스 워커를 수정했다.
- README, 아키텍처, 애니메이션 에셋, 저장 형식, 변경 기록 문서를 v14 기준으로 정리했다.
- 코드 MIT License와 이미지 에셋 별도 고지를 추가했다.

## 자동 검사 결과

다음 검사는 압축 직전에 통과했다.

```text
JavaScript 문법 검사: 통과
HTML 참조 45개: 누락 없음
서비스 워커 오프라인 참조 102개: 누락 없음
전체 스크립트 부팅: 통과
저장 버전·무공 8종·보스 1종 등록: 통과
6초 전투 고정 틱 실행·적 생성: 통과
다중 레벨업 28회 대기열·선택지 3개: 통과
손상 저장 백업 복구·입력 정규화: 통과
```

검사 명령은 다음과 같다.

```bash
npm run check
npm test
```

## 호환성 관련 주의

기존 전투 규칙을 전면 재작성하면 밸런스와 저장 호환성이 크게 깨질 수 있어, v14는 기존 `combat-runtime.js`를 호환 코어로 유지하면서 밸런스·충돌·풀링·고정 틱·저장·이벤트·애니메이션을 외부 시스템으로 분리했다. 신규 기능은 이벤트 버스와 레지스트리를 사용하지만, 오래된 VFX 및 메타 전투 확장에는 일부 호환 래퍼가 남아 있다. 향후 완전한 ES Module 전환은 이 호환 계층을 단계적으로 제거하는 방식이 안전하다.


## v14.3 하이브리드 VFX 추가 적용

- `assets/vfx/`에 투명 PNG Flipbook과 텍스처 29개를 추가했다.
- `js/vfx/sprite-vfx.js`가 기본 투사체, 발동형 무공, 장판, 보스 패턴, 파티클, 태극검진을 외부 에셋으로 렌더링한다.
- 빔과 트레일은 PNG 텍스처를 길이·각도에 맞춰 변형하고, 피해 판정은 기존 전투 코드가 유지한다.
- 캐릭터·적·초상화·VFX가 `GameAssets`의 동일한 Image 객체를 공유한다.
- `tests/vfx-audit.mjs`에서 PNG 서명·크기·레지스트리·서비스 워커 캐시를 검증한다.
- 서비스 워커 캐시는 `cheonha-v14-3-hybrid-vfx`로 갱신했다.
