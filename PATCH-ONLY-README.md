# 천하 서바이버 v14.15.6 → v14.15.7 패치 전용

이 패치는 **v14.15.6 누적 전체본이 이미 적용된 저장소**에만 사용하세요.
현재 버전이 더 오래됐거나 확실하지 않다면 `v14.15.7-cumulative-root-working-copy.zip` 전체본을 사용해야 합니다.

## Working Copy 적용

1. 이 ZIP을 열면 보이는 파일과 폴더를 GitHub 저장소 최상위(현재 `index.html`이 있는 위치)에 모두 덮어씁니다.
2. 변경 사항을 커밋하고 푸시합니다.
3. GitHub Pages 배포 후 `/BUILD.txt`를 열어 `v14.15.7-cumulative-root`인지 확인합니다.
4. 기존 탭을 닫고 새 탭으로 게임을 한 번 다시 엽니다. 서비스 워커의 이전 캐시를 교체하기 위한 절차입니다.

## 삭제 파일

실행 파일 삭제는 없습니다. 기존 `DEPLOY-WORKING-COPY-v14.15.6.md`는 오래된 안내 문서일 뿐이므로 남아 있어도 게임에는 영향이 없습니다. 원하면 수동으로 삭제하세요.

## 이번 패치 핵심

- 구형 강화 이펙트 DOM/CSS 제거
- 강화 진행·성공·실패 후 강화칸 무기 이미지 재렌더링
- 강화 성공 GIF를 무기 이미지 뒤 레이어에 배치
- 모바일에서 강화 무기 이미지의 표시 상태 강제 보정
- 캐시/서비스 워커 버전 갱신

